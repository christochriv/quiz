<h1>Listing authentications</h1>
<% if @authentications %>
  <% unless @authentications.empty? %>
    <p><strong>Vous pouvez vous connecter en utilisant:</strong></p>
    <div class="authentications">
     <% for authentication in @authentications %>
      <div class="authentication">
       <%= image_tag "#{authentication.provider}_32.png", :size => "32x32" %>
       <div class="provider"><% authentication.provider.titleize %></div>
       <div class="uid"><%= authentication.uid %></div>
       <%= link_to "Supprimer", authentication, :confirm => 'Are you sure you want to remove the authentication option ?', :method => :delete, :class => "remove" %>
       </div>
       <% end %>
       <div class="clear"></div>
      </div>
    <% end %>
    <p><strong>Ajoutez un autre service afin de vous connecter:</strong></p>
  <%else %>
   <p><strong>Inscrivez-vous grâce à l'un de ces services:</strong></p>
  <% end %>

  <a href="/auth/facebook" class="auth_provider">
   <%= image_tag "facebook_64.png", :size => "64x64", :alt => "Facebook" %>
   <div class="clear"></div>

